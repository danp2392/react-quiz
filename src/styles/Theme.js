import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#81d3f9",
      light: "#b5ffff",
      dark: "#4ba2c6",
    },
    secondary: {
      main: "#E0E0E0",
    },
  },
  typography: {
    fontWeightLight: 600,
  },
});
